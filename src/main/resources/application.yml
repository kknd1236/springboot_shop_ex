server:
  port: 9000

spring:
  # MySQL ?? ??
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/boot_shop
    username: root
    password:
  # JPA ??
  jpa:
    hibernate:
      ddl-auto: create # ?????? ??? ??
    properties:
      hibernate:
        show_sql: true # ???? ?? ?? ??
        format_sql: true # ???? ???? ??? ???? ?? ???
    database-platform: org.hibernate.dialect.MySQL57Dialect

  thymeleaf:
    cache:false